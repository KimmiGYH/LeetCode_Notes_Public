# 1711_Count Good Meals_大餐计数 (Medium)



## 解法一：哈希表 Hash Table



**(哈希表) $O(nlogS)$**

- 用哈希表统计每种数字出现的次数。

- 对于数字 `x`，枚举 `2` 的幂次，求出方案数。



**时间复杂度：**

- 哈希表统计的时间复杂度为 `O(n)`。

- 对于每种数字，需要 `O(log⁡S)` 的时间统计答案。其中 `S` 为最大的数字。

- 故总时间复杂度为 $O(nlog⁡S)$。

**空间复杂度：**

- 需要 $O(n)$ 的额外空间存储哈希表。

